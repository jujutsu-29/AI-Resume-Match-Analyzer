{
  "name": "ai-service",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "predev": "if not exist venv (python -m venv venv)",
    "dev": ".\\venv\\Scripts\\pip install -r requirements.txt && .\\venv\\Scripts\\uvicorn main:app --reload --port 8000"
  }
}
