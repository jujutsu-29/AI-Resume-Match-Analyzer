{
  "name": "ai-service",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "if not exist venv python -m venv venv && .\\venv\\Scripts\\pip install -r requirements.txt && .\\venv\\Scripts\\uvicorn main:app --reload --port 8000"
  }
}
